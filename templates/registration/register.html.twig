{# templates/registration/register.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<div class="auth-container">
    <h1>Créer un compte</h1>

    {{ form_start(registrationForm) }}
    <div > {{  form_label(registrationForm.pseudo) }} </div>
    <div class="form-control"> {{  form_widget(registrationForm.pseudo) }} </div>

    <div > {{  form_label(registrationForm.email) }} </div>
    <div class="form-control"> {{  form_widget(registrationForm.email) }} </div>

    <div > {{  form_label(registrationForm.plainPassword) }} </div>
    <div class="form-control"> {{  form_widget(registrationForm.plainPassword) }} </div>

    <div > {{  form_label(registrationForm.imageIdentite) }} </div> {# Le champ photo #}
    <div class=""> {{  form_widget(registrationForm.imageIdentite) }} </div>
    <br>


        <button type="submit" class="btn btn-primary">S'inscrire</button>
    {{ form_end(registrationForm) }}
</div>
{% endblock %}
